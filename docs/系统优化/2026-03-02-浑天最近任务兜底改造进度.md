# 浑天最近任务兜底改造进度（2026-03-02）

## 目标
- 让 `Huntian` 页面在没有路由传参时，能从数据库读取“最近一次已完成任务”的方案信息并展示在卡片中。
- 卡片按钮可回跳 `Tianchou` 并定位到“优化方案区 / 帕累托图区”。

## 当前已完成修改

### 1) 后端接口已完成
- 文件：`backend/app/api/routes/tianchou.py`
- 新增：
  - `LatestCompletedTaskResponse` 响应模型
  - `GET /api/v1/tianchou/tasks/latest/completed`
    - 查询最近一次 `TaskStatus.COMPLETED` 的任务
    - 返回任务信息 + 默认方案（按 `rank asc, id asc` 选取）
- 同时调整：
  - `GET /api/v1/tianchou/tasks/{task_id}/solutions` 增加稳定排序  
    `order_by(ParetoSolution.rank.asc(), ParetoSolution.id.asc())`

### 2) 后端测试已完成（并通过）
- 新增测试文件：`backend/tests/api/routes/test_tianchou.py`
- 用例：
  - 有完成任务时返回最近任务及默认方案
  - 无完成任务时返回 `404` + `"No completed optimization task found"`
- 执行结果：`2 passed`

### 3) 前端服务层与类型已完成
- 文件：`frontend/pages/Tianchou/types/tianchou.ts`
  - 新增 `LatestCompletedTaskResponse`
- 文件：`frontend/pages/Tianchou/services/tianchouService.ts`
  - 新增 `getLatestCompletedTask()`

### 4) Huntian 页面已做的部分改动（未收尾）
- 文件：`frontend/pages/Huntian.tsx`
- 已完成：
  - 新增 `useNavigate` 与 `tianchouService` 引入
  - 新增 `HuntianDecisionContext` / `HuntianRouteState` 类型
  - 新增 `decisionContext` 状态
  - 新增 `formatCurrency` 辅助函数
- 未完成：
  - `useEffect` 内“路由态 + 数据库兜底”逻辑尚未完整落地
  - 卡片区 UI 与回跳逻辑（`handleBackToTianchou`）尚未完整写入
  - 未完成 `Huntian.tsx` 的最终静态检查与联调验证

## 接下来要做什么
1. 在 `frontend/pages/Huntian.tsx` 完成第一段 `useEffect`：
   - 优先读取 `location.state.decisionContext`
   - 次优读取 `optimizationResult.solution` 组装 `decisionContext`
   - 无路由数据时调用 `tianchouService.getLatestCompletedTask()`
2. 新增并接线：
   - `handleBackToTianchou(focus: 'solution' | 'pareto')`
   - `expectedLoss` 展示值
3. 在视图中补齐两个卡片模块：
   - 「本次决策方案」
   - 「预期损失」
4. 回归验证：
   - 后端：`uv run pytest tests/api/routes/test_tianchou.py`
   - 前端：仅针对改动文件做检查（避免被项目全量历史告警淹没）
5. 人工联调路径：
   - 无 `state` 直接进入 `/app/huntian`，验证是否展示最近任务卡片
   - 点击卡片按钮，验证是否回跳并定位 `Tianchou` 对应区块

## 备注
- 本次中断前，后端能力与测试已闭环；前端 `Huntian` 还在收尾阶段，文档用于接续实施。
