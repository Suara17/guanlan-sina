(1) 研究SMT（表面贴装技术）、PCB（印制电路板）制造及3C产品组装生产线的标准工艺流程，梳理各环节的关键设备及其布局逻辑。
(2) 搜索上述三类生产线中常见的质量异常（如虚焊、短路、外观破损）与生产效率异常（如堵塞、等待），专门筛选与“设备布局不合理”、“工序物流路线冗余”或“工位节拍不平衡”相关的案例。
(3) 针对筛选出的异常现象，构建“现象-原因”溯源链条：
(a) 确定不良现象（如SMT贴片偏移、PCB板层压气泡）；
(b) 追溯直接原因（如设备振动、传输带速度不匹配）；
(c) 挖掘深层原因（如设备地基不稳需移位、前后工序距离过远导致温降）。
(4) 针对(3)中的异常，调研并整理具体的“物理空间优化”解决方案，包括：
(a) 设备重排策略（如交换贴片机顺序以平衡负荷）；
(b) 线路优化方案（如改变AGV小车配送路径、调整传送带流(1) 研究SMT（表面贴装技术）、PCB（印制电路板）制造及3C产品组装生产线的标准工艺流程，梳理各环节的关键设备及其布局逻辑。

(1) 分析针对SMT、PCB及3C产品生产线的工业故障诊断知识图谱（Knowledge Graph）的通用本体模型（Ontology），特别是如何定义“异常现象”、“多级原因”（根因分析）、“解决方案”、“设备”、“零件”等实体及其相互关系（如：CAUSED_BY, SOLVED_BY, LOCATED_AT）。
(2) 设计并定义用于数据收集的标准输入格式（如Excel模板或JSON Schema），具体包含字段：异常名称、异常描述、涉及产线/设备、多级原因层级（直接原因、根本原因）、解决方案步骤等，以便指导同事进行数据整理。
(3) 调研Neo4j的数据导入流程，制定如何将收集到的结构化数据（CSV/JSON）转换为图谱节点的ETL策略，包括处理多级原因链路和实体去重。
(4) 规划后端API的输出数据格式（通常为JSON），设计Cypher查询语句以获取指定“异常现象”的中心节点及其关联的左侧原因路径和右侧解决方案节点，确保前端能解析出层级结构。
(5) 评估适用于前端网页展示知识图谱的JavaScript库（如D3.js, Cytoscape.js, Neovis.js, ECharts），重点考察其自定义布局能力，以实现“中间现象、左侧原因、右侧方案”的定向拓扑布局。
(6) 研究如何在前端实现图谱节点的交互功能（如点击节点展开/折叠、高亮路径）以及如何将图数据同步转化为下方的文本详情展示（推荐方案列表、原因概率排序等）。
(7) 综合上述调研，总结出一套从数据采集模板、数据库建模、API设计到前端可视化开发的完整实施路径。